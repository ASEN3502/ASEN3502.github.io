source "https://rubygems.org"

# Matches the GitHub Pages build environment, so `bundle exec jekyll serve`
# locally renders the same as the deployed site.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
